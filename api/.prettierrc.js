module.exports = {
	printWidth: 100,                   /* (Default: 80) Specify the line length that the printer will wrap on. */
	tabWidth: 4,                      /* (Default: 2) Specify the number of spaces per indentation-level. */
	useTabs: true,                    /* (Default: false) Indent lines with tabs instead of spaces. */
	// semi: true,                       /* (Default: true) Print semicolons at the ends of statements. */
	// singleQuote: false,               /* (Default: false) Use single quotes instead of double quotes. */
	// quoteProps: "as-needed",          /* (Default: "as-needed") Change when properties in objects are quoted. <as-needed|consistent|preserve> */
	// jsxSingleQuote: false,            /* (Default: false) Use single quotes instead of double quotes in JSX. */
	trailingComma: "all",             /* (Default: "es5") Print trailing commas wherever possible when multi-line. <es5|none|all> */
	// bracketSpacing: true              /* (Default: true) Print spaces between brackets in object literals. */
	// jsxBracketSameLine: false         /* (Default: false) Put the '>' of a multi-line JSX element at the end of the last line */
	// arrowParens: "always"             /* (Default: "always") Include parentheses around a sole arrow function parameter. <always|avoid> */
	// rangeStart: 0,                    /* (Default: 0)        | These two options can be used to format code starting and ending at a given character offset, */
	// rangeEnd: Infinity,               /* (Default: Infinity) | inclusive and exclusive, respectively. These options cannot be used with cursorOffset. */
	// parser: "",                       /* /!\ Prettier automatically infers the parser from the input file path, so you shouldn't have to change this setting. */
	// filepath: "",                     /* (Default: None) Specify the file name to use to infer which parser to use. `cat foo | prettier --stdin-filepath foo.css` */
	// requirePragma: false,             /* (Default: false) Prettier can restrict itself to only format files that contain a pragma, at the top of the file. (See doc) */
	// insertPragma: false,              /* (Default: false) Prettier can insert a special @format marker at the top of files specifying that the file has been formatted with prettier. */
	// proseWrap: "preserve",            /* (Default: "preserve") Controls if prettier also wraps lines in markdown files <always|never|preserve> */
	// htmlWhitespaceSensitivity: "css", /* (Default: "css") Specify the global whitespace sensitivity for HTML files <css|strict|ignore> */
	// vueIndentScriptAndStyle: false,   /* (Default: false) Whether or not to indent the code inside <script> and <style> tags in Vue files. */
	// endOfLine: "lf",                  /* (Default: "lf") Sets line endings flavor. <lf|crlf|cr|auto> */
};
